//test
export const token = state => {
    return state.token
}

export const banks = state => {
    return state.banks
}

export const times = state => {
    return state.times
}

export const bank = state => {
    return state.bank
}

export const time = state => {
    return state.time
}
